#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char m;

	for (m = 'a'; m <= 'z'; m++)
	putchar(m);
	putchar('\n');
	
	return (0);
}
